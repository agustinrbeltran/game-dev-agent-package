# APM — installed dependencies (generated by `apm install`)
apm_modules/

# APM — compiled per-target output (generated by `apm install` / `apm compile`)
/.claude/
/.github/
/.cursor/
/.codex/
/.gemini/
/AGENTS.md

# Editor / OS
.idea/
.DS_Store
